// const btn = document.querySelector("button");

// a comment which not compiled beacuse removeComments is "true"
// btn.addEventListener("click", () => {
//   console.log("clicked!");
// });

let mahdi: Object;

function add(num1: number, num2: number, ageInput: number) {
  let age: number;
  if (num1 > num2)
   return num1 + num2;
}

