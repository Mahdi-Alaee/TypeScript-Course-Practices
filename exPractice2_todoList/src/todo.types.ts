export interface TodoType {
  id: string;
  title: string;
  isComplete: boolean;
}

export interface sweetAlert2Result {
  isConfirmed: boolean;
  isDenied: boolean;
  isDismissed: boolean;
  value: boolean;
}
